import React, { Fragment } from 'react'
import Loading from './Loading'
const Spinner = () => {
    return (
        <Fragment>
            <Loading />
        </Fragment>
    )
}

export default Spinner
